generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Phase 1 scaffold only:
// Data models and migrations are intentionally deferred until Phase 2.
